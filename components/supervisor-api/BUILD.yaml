packages:
  - name: proto
    type: generic
    srcs:
      - "*.proto"
    config:
      commands:
        - ["git", "clone", "https://github.com/googleapis/googleapis"]
        - ["mkdir", "third_party"]
        - ["rm", "-r", "googleapis/.git"]
        - ["mv", "googleapis", "third_party"]
